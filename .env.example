# Copy to .env and fill in real values. Never commit .env (see CLAUDE.md
# section 2 ground rule 7). The same keys are set as GitHub Actions secrets
# for daily.yml.

# Free API key from https://twelvedata.com/ — used for US equity/ETF closing
# prices only. Its free tier excludes the ASX, which is priced via Yahoo
# Finance and needs no key (CLAUDE.md section 3 decision log, 2026-09-20).
TWELVE_DATA_API_KEY=
